# {{PROJECT_NAME}}

## Quality Gates

Run before every `git push` via hooks. Commands read directly from this section.

- **test**: `{{TEST_COMMAND}}` — on fail: stop
- **typecheck**: `{{TYPECHECK_COMMAND}}` — on fail: stop
- **lint**: `{{LINT_COMMAND}}` — on fail: warn
- **build**: `{{BUILD_COMMAND}}` — on fail: stop

## Git

- **Base branch**: `{{BASE_BRANCH}}`
- **Branch prefix**: `feat/`
- **Commit convention**: `type(scope): message`

## Known Issues

<!-- Add only non-obvious constraints, deprecated patterns, and production hazards.
     Delete this section if empty. Do not add anything discoverable from the code. -->
